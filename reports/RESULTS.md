# Results

This file is refreshed by `python scripts/run_all.py`.

The canonical metrics are stored in `reports/metrics/` and the README displays
the key values. Figures are stored in `reports/figures/`.

If you change a generator, random seed, preprocessing step or hyperparameter,
rerun the complete benchmark and commit the refreshed outputs together with the
code change.
